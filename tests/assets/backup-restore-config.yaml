keycloak:
  auth:
    realm: master
    client_id: admin-cli
    client_secret: add-secret-here
    auth_url: http://localhost:8000/auth

storage:
  # s3:
  #   endpoint: http://localhost:9000
  #   access_key: admin
  #   secret_key: password
  #   bucket: backup-restore
  #   region: us-east-1
  local:
    base_dir: /data/backup-restore
